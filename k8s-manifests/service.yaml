apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: vkpaints-dev-ns
spec:
  selector:
    app: notification-service
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
